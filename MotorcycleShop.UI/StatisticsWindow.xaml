<Window x:Class="MotorcycleShop.UI.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:local="clr-namespace:MotorcycleShop.UI"
        xmlns:converters="clr-namespace:MotorcycleShop.UI.Converters"
        mc:Ignorable="d"
        Title="Статистика продаж" Height="800" Width="1200">
    <Window.Resources>
        <converters:DateOnlyConverter x:Key="DateOnlyConverter"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Статистика продаж мотоциклов"
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>

        <!-- Панель фильтрации -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Период с:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker x:Name="StartDatePicker" 
                        SelectedDate="{Binding StartDate, Converter={StaticResource DateOnlyConverter}, ConverterParameter=DateTime}"
                        Width="120" Margin="0,0,15,0"/>
            <TextBlock Text="по:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker x:Name="EndDatePicker" 
                        SelectedDate="{Binding EndDate, Converter={StaticResource DateOnlyConverter}, ConverterParameter=DateTime}"
                        Width="120" Margin="0,0,15,0"/>
            <Button x:Name="ApplyFilterButton" 
                    Content="Применить фильтр" 
                    Width="130" 
                    Height="30"
                    Click="ApplyFilterButton_Click"/>
            <Button x:Name="ResetFilterButton" 
                    Content="Сбросить" 
                    Width="100" 
                    Height="30"
                    Margin="10,0,0,0" 
                    Click="ResetFilterButton_Click"/>
        </StackPanel>

        <!-- Контейнер для диаграмм -->
        <TabControl Grid.Row="2">
            <TabItem Header="По статусам заказов">
                <oxy:PlotView Model="{Binding StatusPlotModel}" Margin="10"/>
            </TabItem>
            <TabItem Header="По месяцам">
                <oxy:PlotView Model="{Binding MonthPlotModel}" Margin="10"/>
            </TabItem>
            <TabItem Header="Популярные мотоциклы">
                <oxy:PlotView Model="{Binding PopularMotorcyclesPlotModel}" Margin="10"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>